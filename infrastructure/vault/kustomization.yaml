apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - vault-tls.yaml
  - vault-release.yaml
configMapGenerator:
  - name: vault-values
    namespace: vault
    files:
      - values.yaml=vault-values.yaml
generatorOptions:
  disableNameSuffixHash: true