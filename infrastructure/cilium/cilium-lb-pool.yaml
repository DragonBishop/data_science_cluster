apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  name: postgis-lb-pool
spec:
  blocks:
    - start: "192.0.2.240"   # confirm before use — see above
      stop: "192.0.2.240"
  serviceSelector:
    matchLabels:
      app.kubernetes.io/name: postgis-external