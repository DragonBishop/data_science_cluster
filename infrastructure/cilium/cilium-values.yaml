# Cilium chart values — pinned against Cilium 1.20.0 / Gateway API v1.6.1.
# Both versions move together; see step 2a before bumping either.

kubeProxyReplacement: true

# Required because k3s runs with --disable-kube-proxy. Nothing exists to resolve
# the kubernetes ClusterIP, so the agent needs the API server endpoint directly.
k8sServiceHost: "127.0.0.1"
k8sServicePort: 6443

operator:
  # Single-node cluster. The default of 2 plus the operator's anti-affinity
  # rule leaves the second replica permanently Pending.
  replicas: 1

l2announcements:
  enabled: true

ipam:
  operator:
    # Must match k3s --cluster-cidr.
    clusterPoolIPv4PodCIDRList:
      - "10.42.0.0/16"

gatewayAPI:
  enabled: true

hubble:
  relay:
    enabled: true
    tls:
      server:
        enabled: true
        mtls: true
  ui:
    enabled: true
  tls:
    auto:
      enabled: true
      method: certmanager
      certManagerIssuerRef:
        group: cert-manager.io
        kind: ClusterIssuer
        name: hubble-ca-issuer