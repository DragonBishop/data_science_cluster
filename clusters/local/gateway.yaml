apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway
  namespace: flux-system
spec:
  interval: 1h
  dependsOn:
    - name: cilium
    - name: cert-manager
    - name: namespaces
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/gateway
  prune: true
  wait: true
  timeout: 3m
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-config
